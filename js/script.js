let cookes = document.getElementById("cookes");
    function show(){
        //cookes.classList.add('visible'); 
        cookes.className = cookes.className.replace('show', '');
    }
    //setTimeout(show, 2000);
    // function close(){
    //     cookes.classList.remove('visible')
    //     cookes.classList.add('invisible');
    // }